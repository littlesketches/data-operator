<script>
    let {isAside = false} = $props() 
</script>


<!-- HTML COMPONENT MARKUP -->
<p>In the <i>Data Operator</i> uses one selected musical scale to harmonise tonal tracks. These are data mapped melodic note sequences on Group A (lead), B (bass) and C3 ("chords"). You can select the scale type and root note by cycling forward and back through a preset list of options.</p>

<div class = 'pad__container'>
    <div class = 'numpad pad'>
        <div class = 'pad-button inactive'>7</div>
        <div class = 'pad-button '>8</div>
        <div class = 'pad-button '>9</div>
        <div class = 'pad-button inactive'>-</div>

        <div class = 'pad-button inactive'>4</div>
        <div class = 'pad-button'>5</div>
        <div class = 'pad-button'>6</div>
        <div class = 'pad-button inactive'>+</div>

        <div class = 'pad-button inactive'>1</div>
        <div class = 'pad-button inactive'>2</div>
        <div class = 'pad-button inactive'>3</div>
        <div class = 'pad-button inactive'><span class="material-symbols-outlined">function</span></div>

        <div class = 'pad-button active'>&times;</div>
        <div class = 'pad-button inactive'>0</div>
        <div class = 'pad-button inactive'>.</div>
        <div class = 'pad-button inactive'><span class="material-symbols-outlined">shift_lock</span></div>
    </div>

    <div class = 'pad-aside'>
        <div class = 'aside-left'>
            <p>&larr; Hold down the select key <span class = 'key'>&times;</span>  and use these  keys for to cycle through available scale types <span class = 'key'>8</span> / <span class = 'key'>9</span> and the to change the root note of the scale with <span class = 'key'>5</span> / <span class = 'key'>5</span> 
            </p>
        </div>
    </div>
</div>



<!-- STYLES-->
<style>
    .pad__container{
        max-width:          55%;
        display:            grid;
        grid-template-columns: 1fr 1fr 1fr;
        column-gap:         2vh;
    }

    .pad-aside p{
        line-height:        1.5;
        font-style:         italic;
        font-size:          smaller;
        margin-block-start: 0;
        margin-block-end:   0;
    }

    .aside-right{
        text-align:         end;
        margin-top:         1rem;
    }

    .pad{
        display:            grid;
        width:              min-content;
        grid-template-columns: repeat(4, 1fr);
        grid-template-rows: repeat(4, 1fr);
        column-gap:         1vh;
        row-gap:            1vh;
    }

    .pad-button{
        width:              2rem;
        aspect-ratio:       1 / 1;
        border:             0.5px solid #c3c3c3;
        display:            flex;
        justify-content:    center;
        align-items:        center;
    }

    .active{
        background:         var(--col-highlight);
        color:           #000;
    }

    .inactive{
        opacity:            0.25;
    }
</style>
